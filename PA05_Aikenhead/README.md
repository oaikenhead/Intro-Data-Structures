# To compile this project, be sure wherever it is installed that the directories in CMakeCache.txt in the build folder match
# change to the build directory
> cd build

# now build the project
> cmake ..
> make all
> make install

# run the executable
> ./proj5
